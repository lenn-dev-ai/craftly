# Mobile-Env — kopieren nach .env und Werte einsetzen
# (.env ist in .gitignore — niemals committen)

# Cloud-Supabase-Werte (für Beta-Testing) oder lokale Instance
EXPO_PUBLIC_SUPABASE_URL=https://gkojaogdzzyuboajwyom.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=
