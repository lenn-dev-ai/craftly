# ARCHIVED / OBSOLETE

Diese Datei ist historisch und nicht mehr operative Source of Truth.

Aktuelle Quellen:
- `ai/REPARO_OPERATING_SYSTEM.md`
- `ai/SESSION_HANDOFF.md`

Nicht für neue Architektur- oder Produktentscheidungen verwenden.

---

# Auto-Fix-Sprint — 2026-05-18 14:00

> **Erzeugt von Cowork-K1-QA-Test**
> Status: ⚠️ Enthält 1 BLOCKER (H1). Pre-Beta-relevant.
> Direkt in Claude Code (Reparo-Ordner) reinpasten — folge der Reihenfolge unten.

---

## Kontext (in 3 Sätzen)

Cowork hat K1.1+K1.2+K1.3 (Commits 5e147ce, 0d89a49, 3f3cdc0) auf Production geprüft. Die Annahme-UI ist da, sieht sauber aus — aber der Submit crasht mit 401 Unauthorized, weil eine Server-Route den gleichen `auth.getUser()`-Bug hat wie B1.1. Plus drei Wording-Restwasser-Punkte aus der Auktion-Ära.

## Pflicht-Reads

1. `BETA-FEEDBACK.md` Iteration 6 — die 4 Punkte H1–H4 mit Reproduktion + Fix-Pattern
2. Commit `1fd30db` (B1.1-Fix) — der diente als Pattern-Referenz für H1
3. `app/api/auction/bid/route.ts` (oder ähnlicher Pfad) — die crashende Route

## Phasenplan

### Phase A — Systematik-Pass (BLOCKER, vor allem anderen)

**H1 ist Beta-Blocker.** Aber: vermutlich systematisch über mehrere Routes.

1. **Grep** im Repo: `auth\.getUser\(\)` (ohne Argument) in allen `app/api/**/route.ts`-Files
2. Liste alle Treffer auf — wahrscheinlich sind mehrere Routes betroffen
3. Fix-Pattern (aus B1.1, Commit 1fd30db):
   - Bearer-Token aus `Authorization`-Header lesen
   - `auth.getUser(token)` mit Token-Argument
   - Falls Token fehlt → 401
4. Empfehlung: Helper-Funktion `getUserFromRequest(request)` in `lib/auth/` zentralisieren, in allen Routes nutzen
5. **Test**: Nach dem Fix in Production deployen (Netlify-Credits gucken — Limit erreicht!) ODER lokal `npm run dev` und einen 401-frei-Test machen
6. Commit-Message: `fix(api): apply B1.1 token-arg pattern systematically across server routes`

### Phase B — Wording-Aufräumen (Pflicht, weil sichtbar in Beta)

H2 + H3 + H4 sind alle alte Auktion-Wording-Reste. In 1 Commit zusammen:

- H3: Disclaimer „bis zum Ablauf der Auktion" → „Mit der Annahme bist du an den vereinbarten Termin gebunden."
- H4: Banner „0 Angebote bisher / Gib ein wettbewerbsfähiges Angebot ab" → entfernen oder zu „Sei schnell — wer zuerst annimmt, bekommt den Auftrag" umformulieren
- H2: API-Route `/api/auction/bid` zu `/api/auftraege/annehmen` umbenennen (Frontend-Call-Site mit anpassen)
- Commit: `chore(naming): replace auction-era wording with festpreis-modell`

### Phase C — Verify

- Cowork-QA wiederholt den K1-Test (kommt automatisch wenn du Phase A + B durch hast)
- Bei grün: K1.2 + K1.3 in der gleichen Session testen

## Constraints

- **Pricing-Engine NICHT anfassen.** Phase-0 #11 ist Vollkalkulation, Code dafür ist stabil.
- **Pro Phase max. 1 Klärungsfrage an Lennart** falls was unklar.
- **Netlify-Credits sind aufgebraucht.** Deploys schlagen aktuell fehl. Lennart entscheidet ob Top-Up oder warten — vor jedem Deploy bei ihm rückversichern.
- **B1.1 Helper-Funktion in `lib/auth/`** zentralisieren — keine route-by-route-Duplikation.

## Erster Schritt

Phase A: Grep und Liste der betroffenen Routes. Frag mich danach „Helper-Funktion in `lib/auth/getUserFromRequest.ts` zentralisieren — ok?".

---

## Anhang: DB-Beleg für H1

Top-Eintrag in `public.feedback` (id `dd42b595-6ae0-4d13-83d2-f97a752fdf23`, rolle handwerker, kontext_url `/dashboard-handwerker/angebot/ebdce602-89a5-4b7b-994e-57efd933da7a`) enthält den vollständigen Bug-Report von Cowork-QA inklusive Netzwerk-Befund.
