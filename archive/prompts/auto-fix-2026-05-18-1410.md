# ARCHIVED / OBSOLETE

Diese Datei ist historisch und nicht mehr operative Source of Truth.

Aktuelle Quellen:
- `ai/REPARO_OPERATING_SYSTEM.md`
- `ai/SESSION_HANDOFF.md`

Nicht für neue Architektur- oder Produktentscheidungen verwenden.

---

# Auto-Fix-Sprint — 2026-05-18 14:10

> Erzeugt vom Auto-Feedback-Loop (Cowork-Testrun).
> Direkt in Claude Code (im Reparo-Ordner) reinpasten.
> Status: 2 auto-fixbare Bugs (1 high, 1 medium). Beide S-Aufwand.

## Pflicht-Reads

1. `BETA-FEEDBACK.md` Iteration 7 — M5 + H5 mit Reproduktion
2. Bei H5: Commit `29626eb` als Pattern-Referenz (Verwalter-KPI-Click-Through F9)

## Phasenplan

### Phase A — H5: HW-KPI „Offene Ausschreibungen" klickbar machen (Bug/high)

**Beobachtung:** Auf `/dashboard-handwerker` zeigt die linke große KPI-Kachel „1 verfuegbar im Radius — offene Ausschreibung" (siehe Screenshot ID `af5e426a` in `public.feedback`). Klick reagiert nicht — User erwartet Navigation zur Ausschreibungs-Liste.

**Vergleichs-Pattern:** Verwalter hat den gleichen Klick-Mechanismus seit F9 (Commit `29626eb`) — Kpi-Komponente bekommt einen optionalen `href`-Prop, klickt durch auf gefilterte Liste.

**Fix:**
1. `app/dashboard-handwerker/page.tsx` öffnen — die KPI-Kachel-Komponente finden (vermutlich `<Kpi>` oder `<KpiCard>` mit `label="VERFUEGBAR IM RADIUS"`)
2. Prüf ob `Kpi`-Komponente schon `href`-Prop unterstützt (sollte sie seit F9). Wenn nicht: erst Prop hinzufügen, dann nutzen.
3. Href = vermutlich `/dashboard-handwerker/ausschreibungen` oder `/dashboard-handwerker/marktplatz` oder ein Scroll-Anchor auf die Liste unten. Routing prüfen.
4. Test: als HW eingeloggt, Klick auf die grüne KPI-Kachel → landet auf Ausschreibungs-Liste.

**Commit:** `fix(hw-dashboard): KPI-Kachel "Offene Ausschreibung" klickbar (H5)`

### Phase B — M5: Mieter-Wizard zentrieren (UX/medium)

**Beobachtung:** Auf `/dashboard-mieter/melden` (alle Wizard-Steps) ist die Seite nicht mittig sondern nach rechts verschoben (siehe ID `f6d050a3`).

**Vermuteter Root-Cause:**
- Möglichkeit A: Container hat `ml-auto` statt `mx-auto`
- Möglichkeit B: Sidebar-Layout nimmt Platz weg, aber Content-Container hat keinen ausgleichenden `mx-auto` für die Inhaltsbreite
- Möglichkeit C: max-width gesetzt aber linkes Padding fehlt → optisch rechts-lastig

**Fix:**
1. `app/dashboard-mieter/melden/page.tsx` Container-Klassen prüfen
2. Oder: das umgebende `app/dashboard-mieter/layout.tsx`
3. Erwartetes Klassenmuster: `<div className="max-w-2xl mx-auto px-4">` o.ä.
4. Test: visuell auf Desktop, dann 390 px Mobile

**Commit:** `fix(mieter-wizard): Container zentriert (M5)`

## Constraints

- Pricing-Engine NICHT anfassen
- Niemals ungefragt Schema-Migrationen
- Pro Phase max. 1 Klärungsfrage an Lennart
- Bei Netlify-Deploys: Credits sind seit Top-Up wieder da, geht durch

## Erster Schritt

Phase A: H5-Fix. Falls die Kpi-Komponente schon `href`-Prop hat, ist das ein 1-Zeilen-Fix. Sonst erst die Komponente erweitern (siehe Commit 29626eb-Pattern). Sag mir was du in der KPI-Komponente vorfindest.

---

## Anhang: Cowork-Hinweis

Die Items H1 (siehe `PROMPTS/auto-fix-2026-05-18-1400.md`) sind STILL OFFEN. Falls du Lust hast, kannst du H1 + M5 + H5 in einer Session abarbeiten — Reihenfolge: erst H1 (Blocker), dann H5 (Bug/high), dann M5 (UX/medium).
